
# ToonTask Desktop Widget

**当前版本**: v6.2 (Physics Inertia & Refined UI)
**风格**: Neubrutalism Cartoon Style

这是一个单文件的桌面待办事项小组件，无需安装 Node.js，无需构建，开箱即用。

## 📁 核心文件
- **index.html**: 包含了所有的逻辑、样式、组件和图标。你只需要运行这就行了。

> 注意：请忽略项目中的 `App.tsx`, `components/` 等文件，它们已废弃。

## ⚙️ 参数调整指南 (Configuration)

你可以在 `index.html` 的代码中搜索 `[CONFIG]` 关键词来快速调整参数。

### 1. 滚轮手感 (Physics & Friction)
- **位置**: `WheelPicker` 组件内
- **INERTIA_DECAY**: 默认 `0.95`。
  - 控制惯性滑动的停止速度。范围 0.0 ~ 1.0。
  - `0.99` = 非常滑 (冰面)。
  - `0.80` = 阻尼大 (泥地)。
- **DRAG_SPEED**: 默认 `0.5`。
  - 鼠标拖拽时的跟手程度。

### 2. 颜色主题
- **位置**: `THEMES` 常量
- **说明**: 定义了界面顶部的颜色方案 (柠檬黄、薄荷绿等)。

## 🌟 核心功能

### 1. 物理惯性选择器 (Inertia Wheel)
- 模拟真实物理滚轮，支持“甩动”操作。
- 自动吸附，无限循环。

### 2. 统计页面 (Compact Stats)
- 优化了布局，现在无需滚动即可查看图表和近期数据。

### 3. 智能倒计时
- 支持暂停/继续。
- 只有时间归零变红后点击才会完成任务。

## 🚀 运行方法
1. 使用 VS Code 打开文件夹。
2. 右键 `index.html` -> "Open with Live Server"。
